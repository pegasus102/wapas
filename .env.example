# Optional. Only needed if you run scripts/run_batch.py --live, which lets
# the diagnosis agent call the real Claude API on ambiguous events instead
# of using the deterministic offline heuristic / committed cache.
# `make all` / `make batch` (default, no flags) never need this.
ANTHROPIC_API_KEY=